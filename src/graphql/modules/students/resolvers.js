import _ from "lodash";
import jwt from "jsonwebtoken";

import { StudentModel, NoteModel } from "../../../models";

export default {
    Note: {
  
    },
  
    Query: {
  
    },
  
    Mutation: {
      
    }
  }